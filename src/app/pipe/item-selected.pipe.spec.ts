import { ItemSelectedPipe } from './item-selected.pipe';

describe('ItemSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
