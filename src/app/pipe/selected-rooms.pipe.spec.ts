import { SelectedRoomsPipe } from './selected-rooms.pipe';

describe('SelectedRoomsPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedRoomsPipe();
    expect(pipe).toBeTruthy();
  });
});
